# General
alias please="sudo !!"
alias pls="please"
mkcd () { mkdir ${1} && cd ${1}; }

# Networking
alias myip="curl -s http://ipecho.net/plain"

# Git
alias review="git log --oneline --decorate --graph --all"
